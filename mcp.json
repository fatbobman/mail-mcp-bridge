{
  "name": "mail",
  "version": "1.3.4",
  "description": "macOS Mail email access service supporting email content reading, thread retrieval, and attachment extraction",
  "author": "fatbobman",
  "repository": "https://github.com/fatbobman/mail-mcp-bridge",
  "language": "python",
  "runtime": {
    "type": "python",
    "command": "python3",
    "entry": "src/mail_mcp_server.py"
  },
  "tools": [
    "get_email_path",
    "get_thread_paths",
    "read_email",
    "read_thread",
    "extract_attachments",
    "cleanup_attachments"
  ],
  "env_schema": [
    {
      "name": "MAIL_THREAD_MAX_BODY_LENGTH",
      "description": "Maximum body length per email when reading threads (default: 1200). Lower values reduce token usage for large threads.",
      "required": false,
      "default": "1200"
    },
    {
      "name": "MAIL_SINGLE_MAX_BODY_LENGTH",
      "description": "Maximum body length when reading a single email (default: 10000). Higher than thread limit to preserve full content.",
      "required": false,
      "default": "10000"
    },
    {
      "name": "MAIL_KEEP_QUOTE_LINES",
      "description": "Number of quote lines to preserve when stripping quoted content in threads (default: 10). Helps maintain conversation context.",
      "required": false,
      "default": "10"
    },
    {
      "name": "MAIL_ATTACHMENT_PATH",
      "description": "Base temp directory path for attachment extraction (default: /tmp, actual directory will have 'mail-mcp-attachments' subdirectory appended)",
      "required": false,
      "default": "/tmp"
    }
  ],
  "requirements": [
    "mcp>=0.9.0"
  ],
  "tags": ["mail", "email", "macos", "productivity", "attachments"],
  "platforms": ["macos"]
}
